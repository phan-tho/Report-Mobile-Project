# Khuyến nghị: xóa workflow này sau khi dùng ci.optimized.yml.
# Lý do: coverage.yml hiện chạy lại unit test/Jacoco riêng, trong khi ci.optimized.yml
# đã sinh jacocoTestReport và gate JVM coverage trong job build-test.
# Giữ file này chỉ như marker để tránh chạy trùng.
name: Coverage

on:
  workflow_dispatch:

jobs:
  removed:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Coverage đã được gộp vào CI để tránh chạy unit/Jacoco hai lần."
